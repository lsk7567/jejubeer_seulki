import React from 'react'

export default function AboutusSubtitle({title, subtitle}) {
    return (
        <div  className="aboutus-subtitle">
            <p>{title}</p>
            <p>{subtitle}</p>
        </div>
    )
}
